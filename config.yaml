home_assistant:
  url: "http://homeassistant.local:8123"
lm_studio:
  url: "http://localhost:1234/v1"   # if LM Studio is on the Windows host, use its LAN IP
  model: "qwen3-8b"
stt:
  model: "distil-small.en"
  device: "cuda"
tts:
  voice: "af_heart"
server:
  host: "0.0.0.0"
  port: 8765
